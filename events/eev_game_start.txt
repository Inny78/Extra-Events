namespace = eev_start

event = {
	id = eev_start.1
	hide_window = yes
    is_triggered_only = yes
	
	trigger = {
		NOT = {
			# has_global_flag = has_extra_events
			has_global_flag = eev_config_menu
		}
	}
	
	immediate = {
		set_global_flag = has_extra_events
		set_global_flag = eev_no_kaboom
		set_global_flag = eev_config_menu
		country_event = { id = eev_start_menu.1 }
	}
}
